{% extends 'base.html.twig' %}

{% block title %}Edit Artist{% endblock %}

{% block body %}
   <div class="card col-4 m-auto mt-5">
       <div class="card-body">
           <h1>Edit Artist</h1>

           {{ include('artist/_form.html.twig', {'button_label': 'Update'}) }}

           <a class="btn btn-primary" href="{{ path('app_artist_index') }}">back to list</a>

           {{ include('artist/_delete_form.html.twig') }}
       </div>
   </div>
{% endblock %}
